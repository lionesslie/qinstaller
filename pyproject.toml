[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "qinstaller"
version = "1.0.0"
description = "Arch Linux package manager"
authors = [
    {name = "lionesslie"}
]
requires-python = ">=3.10"

[project.scripts]
qinstaller = "qinstaller.main:main"